---

# remove directory
# remove auth-providers

- name: "OpenVPN | Server {{ server_name }} | Remove | Directories"
  ansible.builtin.file:
    state: absent
    path: "{{ item }}"
  loop:
    - "{{ server_base_dir }}"
